{{#if wip}}
  {{#if isFiltered}}
    {{filteredCount}}/{{issuesCount}} of {{wip}}
  {{else}}
    {{issuesCount}} of {{wip}}
  {{/if}}
{{else}}
  {{#if settings.showColumnCounts}}
    {{#if isFiltered}}
      {{filteredCount}}/{{issuesCount}}
    {{else}}
      {{issuesCount}}
    {{/if}}
  {{else}}
    {{#if isFiltered}}
      {{filteredCount}}/{{issuesCount}}
    {{/if}}
  {{/if}}
{{/if}}
