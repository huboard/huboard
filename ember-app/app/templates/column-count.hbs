{{#if wip}}
  {{#if issuesAreFiltered}}
    {{issuesCurrentlyVisible}}/{{issuesCount}} of {{wip}}
  {{else}}
    {{issuesCount}} of {{wip}}
  {{/if}}
{{else}}
  {{#if settings.showColumnCounts}}
    {{#if issuesAreFiltered}}
      {{issuesCurrentlyVisible}}/{{issuesCount}}
    {{else}}
      {{issuesCount}}
    {{/if}}
  {{/if}}
{{/if}}
