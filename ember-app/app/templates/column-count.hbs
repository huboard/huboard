{{#if model.wip}}
  {{#if isFiltered}}
    {{filteredCount}}/{{issuesCount}} of {{model.wip}}
  {{else}}
    {{issuesCount}} of {{model.wip}}
  {{/if}}
{{else}}
  {{#if settings.showColumnCounts}}
    {{#if isFiltered}}
      {{filteredCount}}/{{issuesCount}}
    {{else}}
      {{issuesCount}}
    {{/if}}
  {{else}}
    {{#if isFiltered}}
      {{filteredCount}}/{{issuesCount}}
    {{/if}}
  {{/if}}
{{/if}}
