
    <div class="flex-form-bottom">
      <form {{action 'submit' on="submit"}} class="flex-form">
        <h3>Instructions</h3>
        <p>
          Paste the webhook url below
        </p>
        <p>
          Click the Submit webhook button to create the integration.
        </p>
        <label>
          {{input value=model.attrs.webhookURL placeholder="Webhook url" }}
        </label>
        <button disabled={{disabled}} class="hb-button">Submit webhook</button>
        <div class="with-line"><span>or</span></div>
        <div class="cancel"> <a href="#" {{action 'cancel'}}>Cancel</a> </div>
      </form>
    </div>


