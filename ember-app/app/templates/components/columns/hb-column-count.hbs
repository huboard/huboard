{{#if column.wip}}
  {{#if isFiltered}}
    {{filteredCount}}/{{issuesCount}} of {{column.wip}}
  {{else}}
    {{issuesCount}} of {{column.wip}}
  {{/if}}
{{else}}
  {{#if showColumnCounts}}
    {{#if isFiltered}}
      {{filteredCount}}/{{issuesCount}}
    {{else}}
      {{issuesCount}}
    {{/if}}
  {{else}}
    {{#if isFiltered}}
      {{filteredCount}}/{{issuesCount}}
    {{/if}}
  {{/if}}
{{/if}}
