<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>HuBoard :: Access Denied</title>
	<meta name="description" content="Instant project management for GitHub issues">
  <link rel="shortcut icon" href="/favicon.ico?v=3" />
	<meta name="viewport" content="width=device-width, initial-scale=1">

	<!-- Stylesheets -->
	<link href="//fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,700" rel="stylesheet" type="text/css">
  <link rel="stylesheet" href="<%= stylesheet_path "errors/main.css" %>" />
</head>
  <body>
    <div class="flex-wrap">
      <div class="flex-wrap--left">
        <div class="message">
          <div class="message__top">
          <% if is_owner %>
            <h1> ! </h1>
            <h2> HOLD UP! </h2>
            <h3> That page is unauthorized. </h3>
          </div>
          <div class="message__middle">
            <p>
            You'll need to upgrade your account to get those kinds of permissions.
            </p>
          </div>
          <div class="message__bottom">
            <a href="<%= upgrade_url %>" class="call-to-action">
              UPGRADE ACCOUNT
            </a>
          <% else %>
            <h1> ! </h1>
            <h2> HOLD UP! </h2>
            <h3> That page is unauthorized. </h3>
          </div>
          <div class="message__middle">
            <% if issues_enabled %>
              <p>
                Click <a href="https://github.com/<%= params[:user] %>/<%= params[:repo] %>/issues/new?title=HuBoard%20Subscription%20Request&body=@<%= params[:user] %>%20please%20visit%20https://huboard.com/settings/profile%20to%20enable%20HuBoard%20for%20this%20repo" target="_blank"> here </a> if you would like to open an issue on <%= "#{params[:user]}/#{params[:repo]}" %> requesting access to HuBoard from this account
              </p>
            <% else %>
              <div class="message-resolution">
                <p>
                  Please notify an admin for <a href="https://github.com/<%= params[:user] %>" target="_blank"> <%= params[:user] %> </a> to: 
                </p>
              </div>
              <div class="message-steps">
                <ul>
                  <li> Enable Issue Tracking for <%= "#{params[:user]}/#{params[:repo]}" %> </li>
                  <li> Activate a HuBoard Subscription </li>
                </ul>
              </div>
            <% end %>
          </div>
          <div class="message__bottom">
            <a href="/" class="call-to-action">
             BACK HOME 
            </a>
          <% end %>
          </div>
        </div>
      </div>
      <div class="flex-wrap--right illustration illustration--policeman">
      </div>
    </div>
     <%= render partial: 'shared/fontdeck' %>
  </body>

</body>
</html>


