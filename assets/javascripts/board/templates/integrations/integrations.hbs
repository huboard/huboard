<div class="fullscreen-card" style="padding:20px;">
    <h2> Integrations </h2>
    <div class="flex-form-top"> 
      <ul class="unstyled">
        {{#each integration in integrations}}
          <li>
            <div class="integration">
              <div class="title">
                {{ integration.integration.name }}
              </div>
              <dl>
              {{#each integration.keys }}
              <em>{{key}}</em>: {{value}}
              {{/each}}
            </div>
            <button {{action "removeWebhook" integration}} type="button" class="hb-button hb-button-grey">Delete webhook</button>
          </li>
        {{else}}
          <li>
            <em> Integrations are totally rad, you should add one </em>
          </li>
        {{/each}}
      </ul>
    </div>
    <hr></hr>
    <h3> Add an integration </h3>
    {{ outlet }}
</div>

